import styles from '../styles/prebook.module.css';

export default function PreBook() {
  return (
    <div className={styles.wrapper}>
      <span className={styles.text}>Pre-Launch</span>
    </div>
  );
}
